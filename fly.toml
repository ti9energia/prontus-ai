# Fly.io — Auronis Health (Next.js standalone container).
# Usa o Dockerfile do repo (output: 'standalone', EXPOSE 3000).
# O nome do app precisa ser único no Fly — troque `auronis-health` se já existir
# (ou rode `fly launch --no-deploy` para gerar um nome). Segredos (AUTH_SECRET,
# OWNER_PASSWORD, etc.) NÃO ficam aqui — vão via `fly secrets set` (ver DEPLOY-FLY.md).

app = "auronis-health"
primary_region = "gru"  # São Paulo, alinhado com a região gru1 do Vercel

[build]
  dockerfile = "Dockerfile"

[env]
  NODE_ENV = "production"
  NEXT_TELEMETRY_DISABLED = "1"
  PORT = "3000"
  HOSTNAME = "0.0.0.0"

[http_service]
  internal_port = 3000
  force_https = true
  # Mantém 1 máquina SEMPRE ligada — sem isso, as máquinas param quando ociosas
  # e o cold-start do Next (alguns segundos de boot) causava falhas
  # intermitentes de health check → o usuário via erro e precisava "tentar
  # novamente". Com min=1, sempre há uma máquina saudável recebendo tráfego.
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 1

  [http_service.concurrency]
    type = "requests"
    hard_limit = 250
    soft_limit = 200

  # Liveness/readiness — /api/health é sempre 200 (ver src/app/api/health/route.ts).
  # grace_period generoso p/ o boot do Next standalone não falhar o 1º check.
  [[http_service.checks]]
    interval = "30s"
    timeout = "5s"
    grace_period = "30s"
    method = "GET"
    path = "/api/health"

[[vm]]
  size = "shared-cpu-1x"
  memory = "512mb"
