# DECISÕES

## 2026-07-02 — FASE 8: k6 no CI (não local) + correção da chave de API semeada morta
- **k6 no runner do CI, não local:** mesma tensão e mesma resolução da FASE 7 — o pipeline exige prova por execução, o dono pediu para não subir servidor local. `scripts/k6-ci.sh` sobe o `next start`, espera `/api/health`, roda os cenários e derruba o processo, tudo DENTRO de um runner efêmero do GitHub Actions (`.github/workflows/k6.yml`), nunca na máquina do dono. Workflow SEPARADO do `ci.yml` de propósito: testes de carga são mais pesados e seus números dependem do runner, então não pertencem ao portão de correção por-push — dispara em mudanças de `k6/**` e em `workflow_dispatch` manual.
- **stress.js fora do CI por padrão (decisão honesta sobre o que os números significam):** uma rampa de 150 VUs nos MESMOS 2 núcleos do runner que rodam o gerador de carga mede a contenção de CPU do runner, não a capacidade do app. Incluir smoke+load+spike (sinal limpo e interpretável num runner compartilhado) e deixar stress documentado como "rode contra um deploy real". Igual ao framing do Lighthouse na FASE 6: não fingir que um número de ambiente restrito é um número de produção.
- **Thresholds calibrados para o ambiente, não SLO:** os budgets de latência/erro em `k6/lib/config.js` são deliberadamente generosos porque rodam contra o adapter in-memory num runner de 2 núcleos — o objetivo é "o app se mantém saudável e responsivo sob concorrência", não latência absoluta. Números de produção precisam de um deploy real (`-e BASE_URL=...`), feito na verificação conjunta da FASE 12.
- **Bug real corrigido: chave de API semeada morta.** `DEV_KEY_HASH` em `store.ts` era um placeholder de 62 chars (sha256 válido tem 64) que não correspondia ao sha256 de `sk_test_auronis_dev` como o comentário afirmava — a chave semeada nunca autenticava, e em produção (`next start`, atalho `sk_test_*` off) a API v1 inteira ficava inacessível com o seed. Trocado pelo sha256 real. É a superfície de DEMO (mesma fronteira do login DEMO_MODE — só destranca o dataset de demonstração, e deploy real usa Postgres onde o seed não se aplica), então não enfraquece nenhuma fronteira de segurança real. O teste que "achava a chave pelo hash semeado" codificava o hash quebrado (passava sem validar nada) — reescrito para derivar o hash do token real, virando guarda de regressão de verdade.

## 2026-07-02 — FASE 7: run 6 — causa raiz real do 404 (status 200) achada; ponto de parada da caça a bugs via CI
- **run 6 (28622635064): 9/49 falharam, 40 passaram.** Os 2 fixes do run 5 funcionaram (404 conteúdo certo, e-mail duplicado resolvido). `force-dynamic` no catch-all NÃO resolveu o status 200 do 404 — pesquisa confirmou a causa real: `[locale]/loading.tsx` (compartilhado por toda a árvore `[locale]/`) transmite um esqueleto com status 200 antes do `notFound()` do catch-all executar; uma vez que o streaming começa, o status HTTP fica travado — comportamento documentado do Next.js App Router, não um bug de `force-dynamic`/estático-vs-dinâmico. Registrado com precisão em `PENDENCIAS.md` #11 (separado do #10, já que agora tenho a causa raiz exata, só falta a correção arquitetural).
- **Decisão: este é o ponto de parada para caça a bugs via rodadas de CI nesta fase.** 6 rodadas completas (34→30→30→13→10→9 falhas), mais de uma dúzia de bugs reais confirmados e corrigidos (2 bugs de i18n que quebravam 4 fluxos de produto inteiros, contraste de cor em 3 variantes de botão do app inteiro, estrutura ARIA do tablist, limitação de roteamento do Next.js para 404 aninhado, e 7 bugs no próprio código de teste). Os 9 restantes se dividem em: 6 testes de `landing.spec.ts` + o 404 (status code) — precisam de profiling ao vivo (`PENDENCIAS.md` #10/#11); 1 teste de onboarding-após-relogin — risco de regressão maior que o bug se corrigido às cegas (`PENDENCIAS.md` #7); 1 teste de a11y do workspace — nested-interactive + contraste de `text-muted`, mudanças de UX/design que merecem iteração visual (`PENDENCIAS.md` #8/#9); 1 teste de ⌘K — flaky, mitigado com `waitForLoadState('networkidle')`, sem garantia de 100%.
- **Por que parar aqui é a decisão certa, não desistência:** continuar gastando rodadas de CI (~10-25min cada) para perseguir problemas já diagnosticados como precisando de ferramentas de profiling ao vivo (React DevTools/Chrome DevTools Performance) é exatamente o tipo de esforço que não converge sem iteração visual — cada nova tentativa é uma hipótese que não dá pra testar localmente antes de gastar outra rodada inteira. Todos os 9 itens restantes estão documentados com causa-raiz (quando encontrada) ou hipóteses já descartadas (quando não), prontos para retomada rápida na verificação conjunta.
- **Prova:** typecheck ✅ lint ✅ localmente; run 7 do CI confirma que nada regrediu com os ajustes finais (workspace.spec.ts's networkidle wait) antes do merge.

## 2026-07-02 — FASE 7: run 5 — mais 2 bugs reais corrigidos; instabilidade de DOM na landing fica registrada para a verificação conjunta
- **run 5 (28621884564): 10/49 falharam, 37 passaram, 2 flaky** (melhora de 13→10). Dois bugs novos, reais, corrigidos:
  1. **404 regrediu para status 200:** a rota catch-all criada para o achado do run 4 (`[locale]/[...catchAll]/page.tsx`) renderiza o conteúdo certo, mas o Next.js estava otimizando a rota estaticamente, servindo-a como um 200 cacheado em vez de um 404 real por requisição. Corrigido com `export const dynamic = 'force-dynamic'` na rota catch-all.
  2. **`getByRole('link', {name:'Entrar'})` ambíguo** no teste de e-mail duplicado — casava tanto o link DENTRO do alerta de erro quanto o link permanente "Já tem conta? Entrar" do rodapé da própria página de signup. Escopado para `alert.getByRole('link', ...)`.
- **Achado grande, ainda NÃO resolvido — instabilidade de DOM específica da landing page:** 5-7 testes de `landing.spec.ts` (CTA primário, rodapé, páginas legais, FAQ, seletor de idioma, "loads with hero") falham de forma **consistente e reproduzível** nos runs 4 E 5 — ou seja, não é ruído de contenção de CI (isso já foi descartado: o run 5 tem só 10 falhas totais, ambiente bem mais limpo, e o padrão persiste idêntico). Sintomas variam entre "element(s) not found" (rápido, ~8-9s) e "element is not attached to the DOM" / "detached from the DOM, retrying" (lento, bate os 45s) — ambos indicam que a página sofre re-renderizações/remontagens depois do primeiro paint, derrubando referências de nó que o Playwright já tinha capturado.
  - **Hipóteses investigadas e descartadas:** `MISSING_MESSAGE` (confirmado zero desde o run 3); `getByLabel('Senha')` (não usado em nenhum desses testes); contenção de CI pura (o padrão não mudou entre um run com 30 falhas totais e um com 10).
  - **Hipótese líder, não confirmada:** algo no client-tree da landing causa re-render/remontagem periódica que afeta múltiplas seções (hero, rodapé, FAQ) — possivelmente ligado ao loop de animação do `HeroDemo` (setInterval + estado assíncrono) propagando via um ancestral compartilhado, ou um padrão de hidratação em duas passadas. Sem ferramentas de profiling ao vivo (React DevTools Profiler, Chrome DevTools Performance), diagnosticar a causa exata por leitura de log é o limite do que dá pra fazer com confiança — cada tentativa de hipótese custa uma rodada inteira de CI (~10-25min).
  - **Por que não é bloqueante para fechar a FASE 7:** todos os OUTROS fluxos (autenticação, onboarding, checkout, painel do dono, workspace) estão estáveis e passando de forma consistente — o problema é isolado à landing page pública (marketing), não afeta nenhuma funcionalidade do produto autenticado.
  - **Como resolver:** diagnóstico ao vivo com o dono (React DevTools Profiler aberto durante a navegação na landing) — candidato natural para a verificação conjunta pós-FASE 12, ou uma sessão dedicada se o dono preferir investigar antes.
- **Prova:** typecheck ✅ lint ✅ localmente após as correções; run 6 do CI é a prova de execução real dos 2 fixes.

## 2026-07-02 — FASE 7: 1º run real do E2E achou 3 bugs de produção genuínos (34/49 falharam) — corrigidos, não contornados
- **O que aconteceu:** o primeiro run do job `e2e` no GitHub Actions (PR #57, run 28602783913) rodou de verdade — 23m59s, 49 testes reais em Chromium headless — e 34 falharam. Isso é exatamente o valor da FASE 7: como nenhuma fase anterior jamais renderizou estas páginas num navegador real (diretiva do dono: sem servidor local até agora), bugs invisíveis a typecheck/lint/build/vitest só podiam aparecer aqui.
- **Bug #1 (raiz, o maior): `isLanding` em `src/app/[locale]/layout.tsx` usava denylist, não allowlist.** A lógica era `!url.includes('/app') && !url.includes('/login')` para decidir se a rota recebe o catálogo de mensagens FILTRADO (só `landing/common/nav/pricing/faq/meta/pwa`, por tamanho de bundle) ou o completo. Isso classificava `/signup`, `/onboarding`, `/checkout` e `/owner` como "landing" por omissão — nenhuma dessas 4 rotas jamais foi listada. Resultado real em produção: essas 4 páginas inteiras rodavam com o catálogo ERRADO, e qualquer `useTranslations('auth'|'onboarding'|'checkout'|'owner')` dentro delas lançava `MISSING_MESSAGE` no servidor. **Corrigido:** trocado para uma allowlist expressa de rotas de produto (`PRODUCT_PATHS`), que falha seguro — uma rota nova esquecida aqui cai no catálogo COMPLETO (bundle um pouco maior), nunca mais quebra silenciosamente.
- **Bug #2 (agravante): `error.tsx` (o error boundary de toda rota `[locale]`) também dependia de uma mensagem fora do allowlist.** `HeroDemo` (demo animada do hero da landing) usa `useTranslations('encounter')` — namespace real, reaproveitado da tela clínica de consulta — mas `encounter` também não estava no allowlist da landing, então `HeroDemo` lançava `MISSING_MESSAGE` a cada re-render (a demo anima via `setInterval`/estado, então repetidamente). O `error.tsx` deveria capturar isso e mostrar um fallback estável — só que `error.tsx` TAMBÉM chama `useTranslations('notFound')` para o link "voltar ao início", e `notFound` também estava fora do allowlist. Resultado: o próprio fallback de erro quebrava ao tentar renderizar, então a página nunca estabilizava — Playwright via o botão de CTA sendo "detached from the DOM, retrying" em loop até estourar os 45s de timeout. Isso explica por que até testes sem relação aparente com signup (footer, FAQ, idioma, CTA da landing) falharam: instabilidade de página inteira, não um seletor errado. **Corrigido:** `encounter` e `notFound` adicionados ao `LANDING_NS`.
- **Bug #3 (achado por cálculo, não só pelo teste): contraste insuficiente em botões brancos-sobre-cor no design system inteiro.** O axe (`@axe-core/playwright`) acusou `color-contrast` sério em botões `type="submit"` de landing/login/signup — `bg-brand-600` (#00A8A2) com texto branco mede 2.95:1, abaixo do mínimo AA de 4.5:1. Como `buttonVariants` (`src/components/ui/button.tsx`) é o primitivo COMPARTILHADO de botão do app inteiro, o mesmo cálculo (via script Node com a fórmula real de luminância relativa do WCAG) revelou que `accent` (1.81:1) e `danger` (3.91:1) tinham o mesmo problema, não pego pelo axe só porque nenhuma página varrida neste run os usa visivelmente. **Corrigido:** `primary` → `bg-brand-800`/`hover:bg-brand-900` (6.77:1), `accent` → `bg-accent-700`/`hover:bg-accent-800` (5.36:1), `danger` → `bg-danger-fg`/`hover:brightness-90` (5.41:1, reaproveitando o token `fg` já existente em vez de inventar cor nova) — todos com margem real acima de 4.5:1, não um passe raspando o limite.
- **Por que não é regressão desta fase (mas com uma ressalva honesta):** `git log -p` em `layout.tsx` mostra que `LANDING_NS`/`isLanding` nasceram no commit `7161808` ("Block 19: i18n namespace filter", 2026-06-30), bem antes desta pipeline (FASE 0 começou 07-01) — o denylist (`!url.includes('/app') && !url.includes('/login')`) nunca listou `/signup`/`/onboarding`/`/checkout`/`/owner`, e `encounter`/`notFound` nunca estiveram no allowlist, desde a origem. `button.tsx`'s paleta de botões vem do commit inicial `0f6fa4f` (2026-06-24), também pré-pipeline. **Ressalva:** a FASE 6 desta pipeline (`e0ae907`) TOCOU essa mesma linha de `LANDING_NS` (adicionou `'pwa'`) sem notar os buracos de `encounter`/`notFound` — porque a correção da FASE 6 também foi reativa a um bug achado por leitura de código (PWARegister fora do provider), não por execução em navegador real. Ou seja: mesmo tocando o arquivo, nenhuma fase anterior TINHA COMO ver este bug especificamente, porque nenhuma delas renderizou a página de verdade — exatamente a lacuna que a FASE 7 fecha.
- **Por que corrigir agora em vez de só documentar em PENDENCIAS.md:** o princípio "corrigir antes de avançar" — e o próprio propósito de ter a suíte E2E é ficar verde contra um app que funciona de verdade, não contra um app com bugs conhecidos e ignorados. Documentar sem corrigir teria sido exatamente o tipo de "teatro" que o pipeline proíbe.
- **Prova:** typecheck ✅ lint ✅ 349 testes ✅ build prod exit 0 ✅ localmente após as correções; run 2 do CI (após push) é a prova de execução real — ver EVIDENCIAS/fase7/verificacao.txt para os números antes/depois.

## 2026-07-02 — FASE 7: run 2 do CI mostrou que o fix do Bug #1 não resolveu de verdade — o mecanismo inteiro foi removido
- **O que aconteceu:** com `PRODUCT_PATHS` (a allowlist do Bug #1 acima) já no ar, o run 2 (28616953980) caiu de 34 para 30 falhas — melhora real, mas `MISSING_MESSAGE: auth (pt-BR)` continuou aparecendo em `/signup`, e NOVOS namespaces ausentes apareceram (`roles`, `chrome`, `copilot`, `copilot.name`) — inclusive em rotas que deveriam estar 100% seguras pela allowlist (`/app`). Isso significa que o problema nunca foi "a lista estava incompleta" — foi que `headers().get('x-invoke-path') ?? headers().get('next-url')` nunca continha de forma confiável o pathname esperado, para NENHUMA rota. Ambas as chaves são internas do Next.js, sem contrato documentado — o código original já estava "adivinhando" um comportamento não garantido.
- **Decisão final:** em vez de investigar mais a fundo um mecanismo interno não documentado sem conseguir iterar ao vivo (cada tentativa custa ~24min de CI), removido o filtro inteiro. `[locale]/layout.tsx` agora só faz `const messages = await getMessages()` e entrega o catálogo COMPLETO para toda rota, sempre. Sem `isLanding`, sem `LANDING_NS`, sem `PRODUCT_PATHS`, sem leitura de headers.
- **Custo real medido (não estimado):** catálogo completo pt-BR = 50.0 KB não comprimido; o subconjunto que a landing usava = 10.6 KB — uma diferença de ~39 KB não comprimidos (bem menos depois de gzip/brotli, que o Next.js já aplica por padrão) só nas páginas puramente de marketing (`/`, `/contact`, `/privacy`, `/terms`, `/lgpd` — as únicas que alguma vez se beneficiaram do filtro, já que toda outra rota precisa do catálogo completo de qualquer forma). Considerado um preço pequeno e mensurável por eliminar a classe de bug inteira, permanentemente — nenhuma rota nova pode voltar a quebrar por "esqueceram de listar aqui".
- **Build local não pôde confirmar exit 0 desta correção específica:** 6 tentativas consecutivas de `npm run build` local foram encerradas pelo ambiente no meio da compilação (log mostra `write EPIPE` no worker do Next.js — processo morto de fora, não erro do build em si; `npm run typecheck`/`lint`/`vitest` rodaram normalmente no mesmo intervalo). Prova de build fica por conta do próprio job `quality` do CI (que já passou nos runs 1 e 2 contra versões anteriores deste arquivo) — ver EVIDENCIAS/fase7/verificacao.txt, run 3.

## 2026-07-02 — FASE 7: run 3 confirma o fix de i18n (zero MISSING_MESSAGE) — mas revela um bug real no MEU PRÓPRIO teste
- **Boa notícia:** run 3 (28619852561) teve **zero** ocorrências de `MISSING_MESSAGE` no log inteiro — a remoção do filtro de mensagens (decisão acima) resolveu a causa raiz de verdade, confirmado por execução real, não por inspeção de código.
- **30/49 ainda falharam — MAS a lista é idêntica à do run 2, e a causa dominante é outra.** 65 das falhas mostram `strict mode violation: getByLabel('Senha') resolved to 2 elements` — o campo de senha real E o botão "Mostrar senha" (que tem `aria-label="Mostrar senha"`) — porque `getByLabel()` do Playwright faz correspondência por SUBSTRING por padrão (sem `{ exact: true }`), e "Mostrar senha" contém "senha". **Isso é um bug no meu próprio código de teste** (`e2e/helpers.ts`, `e2e/auth.spec.ts`, `e2e/owner.spec.ts`, `e2e/errors-and-regressions.spec.ts`), não um bug do app — passei a exigir `{ exact: true }` em todo `getByLabel('Senha')`. Como `signUpFreshUser`/login são usados pela maioria dos specs, este único bug de seletor explica a maior parte das 30 falhas (checkout, onboarding, auth, parte do a11y).
- **Achado ainda não resolvido, registrado para acompanhar no run 4:** um punhado de testes de `landing.spec.ts` (CTA primário, footer, FAQ, idioma, páginas legais) falha com sintomas de instabilidade de DOM (`element was detached from the DOM, retrying` / `Element is not attached to the DOM`) que NÃO se explicam por `MISSING_MESSAGE` (confirmado ausente) nem pelo bug de `getByLabel('Senha')` (esses testes não tocam o campo de senha). Hipótese líder: com ~24 testes batendo timeout de 45s + 1 retry (até 90s cada) por causa do bug de seletor acima, o runner de CI (2 cores compartilhados entre 2 workers do Playwright) fica sob contenção pesada o suficiente para até interações simples na landing (rodando no worker concorrente) não estabilizarem a tempo — não necessariamente um bug do app. Decisão: corrigir o bug de seletor primeiro (causa confirmada, ação clara) e reavaliar esse achado com dados novos do run 4 antes de investigar mais a fundo — evita perseguir um sintoma que pode desaparecer sozinho.
- **Achado à parte, mais concreto:** o teste de página 404 falha rápido (~9s, não 45s) porque `getByRole('heading', {name: 'Página não encontrada'})` nunca aparece, mesmo com `res.status() === 404` confirmado e `src/app/[locale]/not-found.tsx` parecendo correto por leitura de código. Não existe `src/app/not-found.tsx` na raiz — suspeita (não confirmada) de que o Next.js está caindo no fallback 404 genérico embutido em vez do `not-found.tsx` aninhado em `[locale]/` para esta rota especificamente. Registrado como pendência a investigar se persistir no run 4 — 1 teste isolado, não bloqueia o restante da fase.

## 2026-07-02 — FASE 7: GitHub Actions como ambiente de execução do E2E (não local)
- **Tensão:** o pipeline exige prova por execução real ("sem teatro" — não afirmar que algo funciona sem rodar); o dono pediu para não subir servidor local nesta rodada, com a verificação ao vivo reservada para uma sessão conjunta após a FASE 12.
- **Resolução:** o `webServer` do Playwright (`playwright.config.ts`) sobe o `next start`, espera `/api/health` responder, roda a suíte inteira e derruba o processo sozinho — esse ciclo de vida completo roda DENTRO do runner do GitHub Actions (job `e2e` novo em `ci.yml`), nunca na máquina local. Isso entrega prova de execução genuína (não é leitura de código, é clique-e-resposta real via Chromium headless) sem contradizer "não precisa subir porta nem nada" — a porta sobe e morre num runner efêmero na nuvem, não na máquina do dono.
- **Por que não esperar até a FASE 12:** a suíte E2E É o mecanismo de prova da FASE 7; adiar a execução tornaria a fase inteira "planejada, não verificada" por 5 fases seguidas (8–12), quebrando o princípio de não carregar pendências entre fases. CI resolve isso sem violar a diretiva do dono.
- **Efeito colateral corrigido:** a suíte completa (49 testes) gera ~19 chamadas a `/api/auth/signup` e ~8 a `/api/auth/login` na mesma execução — o suficiente para os rate-limiters de produção (5/15min, 8/15min) derrubarem a própria suíte em cascata. Resolvido tornando os limites configuráveis via `E2E_MAX_LOGIN_ATTEMPTS`/`E2E_MAX_SIGNUP_ATTEMPTS` (só setados em `playwright.config.ts`'s `webServer.env`; os defaults de produção — 5/8 — não mudam).
- **Precedente para FASE 8 (k6):** mesma tensão vai se repetir para testes de carga. Plano: tentar o mesmo padrão (rodar dentro do runner do CI) antes de considerar deferir para a verificação conjunta.

## 2026-07-02 — Incidente: 3 agentes de UI em paralelo retornaram sem produzir arquivos
- Delegados 3 agentes em background para construir UI de signup/onboarding/checkout (specs completas, contratos de API prontos). Os três voltaram com mensagem atípica: "You've hit your session limit · resets 5am (America/Fortaleza)" e, em dois casos, aviso de que o classificador de segurança não pôde revisar o trabalho.
- **Verificação:** `git status` após os três terminarem mostrou ZERO arquivos novos/alterados além dos meus próprios — nenhuma das três frentes produziu qualquer artefato persistido, apesar de reportarem 62–91 chamadas de ferramenta cada. Conclusão: o limite de sessão interrompeu os agentes antes de qualquer escrita durável (ou o trabalho não foi persistido por algum outro motivo) — não confiei no "resultado" reportado, tratei como se nada tivesse sido feito.
- **Decisão:** não tentei novos agentes em background (risco de bater no mesmo limite outra vez, desperdiçando mais). Constatei que o reset (5h em Fortaleza, UTC-3) já tinha passado no horário corrente (~14h UTC) — o próximo reset ficaria a ~17-18h de distância. Passei a construir as três telas (signup, onboarding, checkout) diretamente eu mesmo, sequencialmente, reaproveitando o mesmo contexto/specs que dei aos agentes. Terminado com sucesso: build de produção com exit code 0 (confirmado via Bash — o PowerShell reportou 255 por engano, interpretando uma linha de warning do Next.js como erro), typecheck/lint limpos, 349 testes de backend seguem verdes.
- **Aprendizado registrado:** ao delegar tarefas grandes (>10min) em paralelo, verificar sempre `git status`/diff real após a conclusão antes de confiar no texto do resultado — especialmente quando a mensagem final foge do padrão esperado.

## 2026-07-02 — Demo button: não reintroduzir na UI de signup
- Ao construir `signup-form.tsx`, notei que `login-form.tsx` não tem mais botão de demo visível (embora `DEMO_MODE`/`demoIdentity()` sigam funcionais no backend, e `auth.demoNote`/`auth.enterDemo` sigam nos catálogos de i18n, agora órfãos). Confirmado na minha memória persistente (`landing-design-directives`): o dono já pediu explicitamente a remoção do botão de demo em sessão anterior — não é bug, é decisão de produto. Não adicionei nenhum atalho de demo na tela de cadastro nem reintroduzi o botão no login.
- As chaves i18n órfãs (`auth.demoNote`, `auth.enterDemo`) e o `GETTING-STARTED.md` (que ainda descreve o botão de demo) ficam para a FASE 11 (limpeza/documentação) — fora do escopo desta fase.

## 2026-07-02 — FASE 5: escopo do signup/onboarding frente ao modelo de dados atual
- **Achado:** todo o núcleo clínico (`listPatients/listEncounters/notes/guides/labOrders`) é um dataset global único, não segmentado por `orgId` — é assim para TODAS as identidades hoje (owner, médico-teste, demo), não é uma lacuna introduzida pelo signup. Só as entidades voltadas ao dono (`tenants`, `users` via `orgId`, `apiKeys`) já são multi-tenant de verdade.
- **Decisão:** o signup cria Tenant + User REAIS (persistidos, aparecem no painel do dono, login funciona de verdade); o onboarding persiste progresso REAL por org; o checkout cria Order/Subscription REAIS por tenant. Ao entrar em `/app`, o novo usuário vê o mesmo workspace clínico de demonstração compartilhado que médico-teste/demo já veem hoje — isso é consistente com o produto atual, não uma mentira nova.
- **Por que não isolar dados clínicos por tenant agora:** exigiria re-arquitetar dezenas de funções do store (patients/encounters/notes/guides/labs) que hoje assumem um único contexto — uma migração de arquitetura própria, desproporcional ao pedido desta fase (integrações pré-prontas). Registrado como fronteira consciente em `PENDENCIAS.md`, candidato a uma fase própria se o dono priorizar multi-tenancy clínica completa.
- A sessão (`useSession()`) já reflete a identidade real de quem logou (nome/e-mail) na top-bar — só o *conteúdo clínico* de demonstração é compartilhado, a identidade de quem está logado é sempre verdadeira.

## 2026-07-02 — FASE 5: Mercado Pago como provedor de referência do checkout
- Adapter real implementado contra a API do Mercado Pago (PIX + boleto + cartão nativos em uma API só, documentação estável, mais usado no Brasil) — mesmo padrão dos demais seams (Memed/ICP/WhatsApp): só ativa com `MERCADOPAGO_ACCESS_TOKEN`; sem a chave, cai no mock em sandbox determinístico. Nenhuma chamada real ocorre sem credencial (princípio 5 — pré-pronto é honesto).

## 2026-07-02 — FASE 4: `screens/integrations.tsx` fica session-only (decisão consciente, não lacuna)
- Considerei ligar o toggle conectar/desconectar e o modal de config ao `upsertTenantConnector` real (o mesmo usado pela `AiSection` do owner) para sobreviver a reload.
- Decidi NÃO fazer isso: dos 20 provedores listados nessa tela (Tasy, MV Soul, iClinic, Feegow, Unimed, Bradesco, SulAmérica, Amil, Hapvida, Auronis ASR, Whisper, Azure Speech, Google Speech, WhatsApp Business, Telegram), nenhum tem conector real implementado em `lib/connectors` — os conectores reais do produto são Memed, ICP-Brasil e WhatsApp Cloud (documentados via env var, `INTEGRACOES.md` virá na fase 5). Persistir esse toggle no servidor seria "persistência-teatro": sobreviveria a reload sem passar a fazer nada a mais.
- A tela já rotula honestamente "Superfície de demonstração — as credenciais ficam apenas neste navegador, nesta sessão" — mover para o store do servidor tornaria essa frase falsa.
- Registrado como ⚠️ justificado em `INVENTARIO.md`, não como pendência.

## 2026-07-01 — FASE 2: alvo estético e priorização (auditorias em EVIDENCIAS/design-antes/)

**Alvo estético:** premium clínico dark-first já estabelecido (turquesa #14C8C4 + prata, Sora/Inter, profundidade por sombra contida e aurora) — NÃO trocar a identidade; **remover o que a contradiz** (violeta off-brand no DNA), **dar vida às telas administrativas chapadas** reaproveitando os padrões que as telas clínicas já provam (sparkline, Progress, StatCard, stepper, Avatar), e **fechar os buracos de confiança** (preços divergentes, links mortos, toasts que mentem, formulário que descarta edição).

**Priorização (impacto × esforço) — vira o plano da FASE 3/4:**
- **P0 (confiança/correção):** preços Pricing×ROI divergentes; links legais mortos (privacy/terms/lgpd/contact); perda silenciosa no form TISS; destrutivas sem confirmação (criar ConfirmDialog único); CMS de landing que finge publicar; seções do owner que não persistem (tenants/plans/access/whatsapp); erro do chat Mari engolido em '…'.
- **P1 (sistema):** i18n unificado (eliminar L()/COPY → messages/*.json); FeatureCard único p/ cards triplicados; buttonVariants no ROI; tooltip Recharts por token (tema claro); EmptyState nas 5 telas sem; loading nos botões async; violeta→brand no dna-helix; fallback visível sem JS p/ .rv; funil de conversão (CTA sem cadastro → decidir na FASE 5 com onboarding).
- **P2 (polish):** contraste text-subtle ≤12px → text-muted; aria-label PT fixo; reduced-motion no hero-demo/JS; valores mágicos de fonte; ritmo py; variação de reveals; aria-live no pricing; foco preso no menu mobile; ArrowLeft/Right no tablist; enums crus traduzidos; paginação de listas longas (FASE 10).
- **Top-5 sem vida a elevar (FASE 3):** contratos, equipe, requisicao, faturamento, integrations + overview do owner.

## 2026-07-01 — Evidência sem servidor local (diretiva do dono)
- O dono pediu foco 100% no código, sem subir servidor/porta para não pesar a máquina.
- Consequência: evidências das fases de design (2–3) passam a ser análise de código + diffs antes × depois (em vez de screenshots); validação funcional via typecheck/lint/vitest/build; E2E/k6/Lighthouse (fases 7, 8, 10) precisarão de execução — ao chegar lá, rodar de forma pontual e enxuta ou escalar ao dono.

## 2026-07-01 — FASE 1: entradas públicas por módulo com split de runtime
- Cada módulo de `lib/` expõe barrel `index.ts` (isomórfico) e, quando preciso, `server.ts` (Node-only) / `client.tsx` (hooks React). Motivo: um barrel único puxaria `node:crypto`/SDKs para o client e React para o edge middleware.
- `src/lib/auth.ts` (hooks client) movido para `src/lib/auth/client.tsx` para o diretório `lib/auth/` virar o módulo canônico.
- Import profundo cruzado agora é **erro de lint** (`no-restricted-imports`); testes internos do módulo são isentos.
- `lib/config` é a única porta para `process.env` (getters lazy — testes que mutam env seguem funcionando; `NEXT_PUBLIC_*` com acesso literal para inline no client).
- `.env.example` passou a documentar TODAS as vars (DB, ASR, Memed, ICP, WhatsApp) — antes só existiam em comentários de código.

## 2026-07-01 — Convenção de entrega
- Pipeline de 13 fases (0–12) com portões duros; estado em `.entrega/`.
- Cada fase fecha em feature branch → PR → merge (preferência durável do dono), commit atômico conforme ANEXO C.
- Trabalho em PT-BR.
